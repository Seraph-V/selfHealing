# SCENARIO: configurational_regression
# TYPE: Configurational
# BROKEN: Pinned to a nonexistent version of requests.
#
# This file replaces requirements.txt to trigger the pip install failure.
# The agent must correct the version pin.

requests==2.28.1  # Corrected to an existing version
pytest==7.4.3
flake8==6.1.0
PyYAML==6.0.1
